From f45ffe5fb59804f729db73021ba166cc93131263 Mon Sep 17 00:00:00 2001
From: Arena Agent <arena-agent@users.noreply.github.com>
Date: Sun, 23 Aug 2026 15:08:06 +0000
Subject: [PATCH] Sync Supabase PKCE auth and location sync with main website

---
 .env                         |  3 ++
 .env.example                 | 13 ++---
 src/App.tsx                  | 17 +++++++
 src/hooks/useLocationSync.ts | 95 ++++++++++++++++++++++++++++++++++++
 src/lib/mainWebsiteAuth.ts   | 14 ++++++
 src/lib/supabaseClient.ts    | 12 ++++-
 6 files changed, 144 insertions(+), 10 deletions(-)
 create mode 100644 .env
 create mode 100644 src/hooks/useLocationSync.ts
 create mode 100644 src/lib/mainWebsiteAuth.ts

diff --git a/.env b/.env
new file mode 100644
index 0000000..17481e0
--- /dev/null
+++ b/.env
@@ -0,0 +1,3 @@
+VITE_SUPABASE_URL=https://qwaehqsmodekbgvnaavz.supabase.co
+VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF3YWVocXNtb2Rla2Jndm5hYXZ6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODUxNjQ5MjksImV4cCI6MjEwMDc0MDkyOX0.K92b2vkEb77dyu8fYYZpMTIbTyP98Vo80TaMo_Hmq_E
+VITE_SUPABASE_STORAGE_KEY=nexora.auth.qwaehqsmodekbgvnaavz
diff --git a/.env.example b/.env.example
index 7b7dc69..eeff322 100644
--- a/.env.example
+++ b/.env.example
@@ -1,15 +1,10 @@
 # GEMINI_API_KEY: Required for Gemini AI API calls.
-# AI Studio automatically injects this at runtime from user secrets.
-# Users configure this via the Secrets panel in the AI Studio UI.
 GEMINI_API_KEY="MY_GEMINI_API_KEY"
 
-# APP_URL: The URL where this applet is hosted.
-# AI Studio automatically injects this at runtime with the Cloud Run service URL.
-# Used for self-referential links, OAuth callbacks, and API endpoints.
+# APP_URL: Optional public origin for app links.
 APP_URL="MY_APP_URL"
 
-# --- Nexora Supabase (shared project qwaehqsmodekbgvnaavz) -----------------
-# Supabase Dashboard → Project qwaehqsmodekbgvnaavz → Settings → API.
-# The app refuses to boot against any other Supabase project.
+# Shared Nexora Supabase project
 VITE_SUPABASE_URL=https://qwaehqsmodekbgvnaavz.supabase.co
-VITE_SUPABASE_ANON_KEY=PASTE_REAL_ANON_KEY_HERE
+VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InF3YWVocXNtb2Rla2Jndm5hYXZ6Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3ODUxNjQ5MjksImV4cCI6MjEwMDc0MDkyOX0.K92b2vkEb77dyu8fYYZpMTIbTyP98Vo80TaMo_Hmq_E
+VITE_SUPABASE_STORAGE_KEY=nexora.auth.qwaehqsmodekbgvnaavz
diff --git a/src/App.tsx b/src/App.tsx
index 96693b5..da42a54 100644
--- a/src/App.tsx
+++ b/src/App.tsx
@@ -11,6 +11,8 @@ import OfflineNotificationBanner from './components/OfflineNotificationBanner';
 import { supabase, supabaseConfigError, initialRecoveryLink } from './lib/supabaseClient';
 import { checkGrowthPartnerAccess } from './lib/gpRepository';
 import ResetPasswordScreen from './components/ResetPasswordScreen';
+import { useLocationSync } from './hooks/useLocationSync';
+import { redirectToMainWebsiteLogin } from './lib/mainWebsiteAuth';
 
 // Heavy route-level screens are code-split so the initial bundle only carries
 // the login + dashboard path instead of every screen in the app.
@@ -103,7 +105,9 @@ export default function App() {
   // role stored in the database — never localStorage, URL or app state.
   const [isLoggedIn, setIsLoggedIn] = useState(false);
   const [authReady, setAuthReady] = useState(false);
+  const [sessionUserId, setSessionUserId] = useState<string | null>(null);
   const authVerifySeq = useRef(0);
+  const hadSessionRef = useRef(false);
 
   // ---- Password recovery (Phase 3) ----
   // 'pending'  = a recovery link was opened, waiting for Supabase to verify it
@@ -144,12 +148,19 @@ export default function App() {
       if (recoveryRef.current === 'pending' || recoveryRef.current === 'active') return;
       const seq = ++authVerifySeq.current;
       if (!session?.user) {
+        setSessionUserId(null);
         if (!cancelled) {
           setIsLoggedIn(false);
           setAuthReady(true);
+          if (hadSessionRef.current) {
+            hadSessionRef.current = false;
+            redirectToMainWebsiteLogin('signed_out');
+          }
         }
         return;
       }
+      hadSessionRef.current = true;
+      setSessionUserId(session.user.id);
       const check = await checkGrowthPartnerAccess(client, session.user.id);
       if (cancelled || seq !== authVerifySeq.current) return;
       if (check.state === 'denied') {
@@ -206,6 +217,12 @@ export default function App() {
   const [selectedTicketId, setSelectedTicketId] = useState<string | null>(null);
   
   const [isOnline, setIsOnline] = useState<boolean>(navigator.onLine);
+
+  useLocationSync({
+    client: supabase,
+    userId: sessionUserId,
+    enabled: Boolean(isLoggedIn && sessionUserId),
+  });
   const [isSyncing, setIsSyncing] = useState<boolean>(false);
 
   // No more fake seeded demo profile / dashboard cache — the dashboard reads
diff --git a/src/hooks/useLocationSync.ts b/src/hooks/useLocationSync.ts
new file mode 100644
index 0000000..b8b9377
--- /dev/null
+++ b/src/hooks/useLocationSync.ts
@@ -0,0 +1,95 @@
+import { useEffect } from 'react';
+import type { SupabaseClient } from '@supabase/supabase-js';
+
+type UseLocationSyncOptions = {
+  client: SupabaseClient | null | undefined;
+  userId: string | null | undefined;
+  enabled?: boolean;
+};
+
+const GEO_OPTIONS: PositionOptions = {
+  enableHighAccuracy: true,
+  timeout: 15_000,
+  maximumAge: 0,
+};
+
+async function writeLocation(client: SupabaseClient, userId: string, position: GeolocationPosition) {
+  const capturedAt = new Date(position.timestamp).toISOString();
+  const latitude = position.coords.latitude;
+  const longitude = position.coords.longitude;
+  const accuracy = Number.isFinite(position.coords.accuracy) ? position.coords.accuracy : null;
+  const attempts = [
+    () => client.from('user_locations').upsert({
+      user_id: userId,
+      latitude,
+      longitude,
+      accuracy_m: accuracy,
+      source: 'gps',
+      captured_at: capturedAt,
+      updated_at: capturedAt,
+    }, { onConflict: 'user_id' }),
+    () => client.from('user_locations').upsert({
+      user_id: userId,
+      latitude,
+      longitude,
+      accuracy,
+      updated_at: capturedAt,
+    }, { onConflict: 'user_id' }),
+    () => client.rpc('save_my_private_location', {
+      p_latitude: latitude,
+      p_longitude: longitude,
+      p_accuracy_m: accuracy,
+      p_altitude_m: position.coords.altitude ?? null,
+      p_altitude_accuracy_m: position.coords.altitudeAccuracy ?? null,
+      p_speed_mps: position.coords.speed ?? null,
+      p_heading_degrees: position.coords.heading ?? null,
+      p_captured_at: capturedAt,
+    }),
+  ];
+
+  let lastError: unknown = null;
+  for (const attempt of attempts) {
+    const { error } = await attempt();
+    if (!error) return;
+    lastError = error;
+  }
+  throw lastError instanceof Error ? lastError : new Error(String(lastError || 'Location sync failed.'));
+}
+
+export function useLocationSync({ client, userId, enabled = true }: UseLocationSyncOptions) {
+  useEffect(() => {
+    if (!enabled || !client || !userId) return;
+    if (typeof navigator === 'undefined' || !('geolocation' in navigator)) return;
+
+    let active = true;
+    let lastSignature = '';
+    let saveChain: Promise<void> = Promise.resolve();
+
+    const watchId = navigator.geolocation.watchPosition(
+      (position) => {
+        if (!active) return;
+        const signature = `${userId}:${position.timestamp}:${position.coords.latitude.toFixed(6)}:${position.coords.longitude.toFixed(6)}`;
+        if (signature === lastSignature) return;
+        lastSignature = signature;
+        saveChain = saveChain
+          .catch(() => undefined)
+          .then(async () => {
+            if (!active) return;
+            await writeLocation(client, userId, position);
+          })
+          .catch((error) => {
+            console.warn('[Nexora] location sync skipped:', error);
+          });
+      },
+      (error) => {
+        console.warn('[Nexora] location permission/status:', error.message);
+      },
+      GEO_OPTIONS,
+    );
+
+    return () => {
+      active = false;
+      navigator.geolocation.clearWatch(watchId);
+    };
+  }, [client, enabled, userId]);
+}
diff --git a/src/lib/mainWebsiteAuth.ts b/src/lib/mainWebsiteAuth.ts
new file mode 100644
index 0000000..f9ee6c5
--- /dev/null
+++ b/src/lib/mainWebsiteAuth.ts
@@ -0,0 +1,14 @@
+const env = (import.meta as ImportMeta & { env?: Record<string, string | undefined> }).env;
+
+export const MAIN_WEBSITE_LOGIN_URL = 'https://nexora-main-website.vercel.app/';
+export const DEFAULT_NEXORA_STORAGE_KEY = 'nexora.auth.qwaehqsmodekbgvnaavz';
+export const NEXORA_STORAGE_KEY = env?.VITE_SUPABASE_STORAGE_KEY?.trim() || DEFAULT_NEXORA_STORAGE_KEY;
+
+export function redirectToMainWebsiteLogin(reason = 'signed_out') {
+  if (typeof window === 'undefined') return;
+  const url = new URL(MAIN_WEBSITE_LOGIN_URL);
+  url.searchParams.set('source', window.location.origin);
+  url.searchParams.set('reason', reason);
+  url.searchParams.set('next', window.location.href);
+  window.location.assign(url.toString());
+}
diff --git a/src/lib/supabaseClient.ts b/src/lib/supabaseClient.ts
index 8e11a14..eaea0dc 100644
--- a/src/lib/supabaseClient.ts
+++ b/src/lib/supabaseClient.ts
@@ -6,6 +6,7 @@
 // Configure via VITE_SUPABASE_URL / VITE_SUPABASE_ANON_KEY (see .env.example).
 
 import { createClient } from '@supabase/supabase-js';
+import { NEXORA_STORAGE_KEY } from './mainWebsiteAuth';
 
 const SUPABASE_PROJECT_REF = 'qwaehqsmodekbgvnaavz';
 const EXPECTED_SUPABASE_HOSTNAME = `${SUPABASE_PROJECT_REF}.supabase.co`;
@@ -130,5 +131,14 @@ export const supabaseConfigError = config.isValid ? null : config.error;
 // users routinely open recovery emails in a DIFFERENT browser/app webview.
 // Password sign-in (grant) is unaffected by this setting.
 export const supabase = config.isValid
-  ? createClient(config.url, config.anonKey, { auth: { flowType: 'implicit' } })
+  ? createClient(config.url, config.anonKey, {
+      auth: {
+        persistSession: true,
+        autoRefreshToken: true,
+        detectSessionInUrl: true,
+        flowType: 'pkce',
+        storageKey: NEXORA_STORAGE_KEY,
+      },
+      global: { headers: { 'x-nexora-client': 'growth-partner-pwa/auth-sync' } },
+    })
   : null;
-- 
2.39.5

