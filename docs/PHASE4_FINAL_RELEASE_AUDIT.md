# Nexora — Phase 4 Final E2E Verification & Deployment Audit

**Date:** 2026-08-04  
**Shared Supabase project:** `qwaehqsmodekbgvnaavz`  
**Main Website branch:** `arena/019fcc8c-nexora-main-website`  
**Audit verdict:** **NO-GO for production release until live environment and Supabase E2E gates are re-run**

## 1. Repository and patch inventory

| Deployment | Locked main inspected | Phase 4 patch state |
|---|---|---|
| Main Website | Current session branch | Architecture gateway + proxy code present |
| Customer PWA | `4eff31469914679f6a5c6443eaf3ad851a883240` | Patch applies cleanly; scoped customer worker |
| Shop Owner PWA | `49ffe780c542dc693269c063cde6185cf5c86b61` | Patch applies cleanly; role gate + live owner screens |
| Growth Partner PWA | `2832af2cb2c478c08b67e18fae2ff370beac419b` | Patch applies cleanly; Phase 3 live identity/proposal/commission flow |

## 2. Architecture and cross-portal routing

**Static/contract verification: PASS**

- Main Website owns public marketing, marketplace, storefront, legal, auth, and
  portal gateway only.
- Main Website no longer renders copied Customer/Owner/Partner dashboards.
- Main Website no longer owns `create_customer_booking`, Razorpay booking writes,
  Growth Partner proposal writes, or Owner proposal review writes.
- Reverse-proxy variables exist for all three portal paths:
  - `NEXORA_CUSTOMER_PWA_ORIGIN`
  - `NEXORA_OWNER_PWA_ORIGIN`
  - `NEXORA_PARTNER_PWA_ORIGIN`
- Canonical path contract is present:
  - `/app/customer/*`
  - `/app/owner/*`
  - `/app/partner/*`
- Legacy `/dashboard/*` and `/booking/*` URLs hand off to canonical portals.
- `/admin/*` is restricted and has no public signup.
- Main Website has no root-scope service worker; `/sw.js` returns 404.

**Live browser cross-portal session verification: BLOCKED**

The sandbox does not contain the deployment origin variables or live test
sessions. Therefore a browser-level sequence across the apex domain could not
be honestly marked PASS.

## 3. Auth and mock/secret audit

**Static source + production bundle audit: PASS**

- Main Website uses only `NEXT_PUBLIC_SUPABASE_URL` and
  `NEXT_PUBLIC_SUPABASE_ANON_KEY`.
- Vite apps use only `VITE_SUPABASE_URL` and `VITE_SUPABASE_ANON_KEY`.
- No active production bundle contains the known hardcoded anon JWT.
- No active production bundle contains the old seeded partner/customer/revenue
  values.
- Customer role-dashboard modules, Owner mock marketing modules, and Growth
  Partner legacy mock dashboard modules are no longer part of the active
  production route/bundle.
- Role checks fail closed on missing, inactive, or wrong-role profiles.
- Growth Partner identity and referral code are generated by the server RPC,
  not by local code.

A placeholder string in an Owner Profile input (`e.g. Your full name`) is UI
placeholder text, not business data.

## 4. Automated validation

| Validation | Result |
|---|---:|
| Main Website `npx tsc --noEmit` | PASS |
| Main Website `npm test` | PASS |
| Main Website contract suite | PASS — 55 tests |
| Full Website architecture contracts | PASS — 7 tests |
| Customer patch apply/typecheck/build | PASS |
| Owner patch apply/typecheck/build | PASS |
| Growth Partner patch apply/typecheck/build | PASS |
| PWA base-path contracts | PASS |
| PWA service-worker scope contracts | PASS |
| Hardcoded JWT scan of active source/bundles | PASS |

## 5. Live Supabase E2E gate

**BLOCKED — not run**

The required live probe could not be executed from this sandbox because:

1. `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` are not present
   in the sandbox environment.
2. Vite Supabase variables and the three PWA origin variables are not present
   in the sandbox environment.
3. Direct HTTPS probes to the Supabase project returned `HTTP 000`
   (`SSL_ERROR_SYSCALL`) in this environment.
4. No live Customer, Owner, or Growth Partner test credentials are stored in
   the repository, correctly.

No fabricated live PASS was recorded.

## 6. Required production smoke sequence before GO

Run this against the deployed apex domain with the user's configured
environment and dedicated test accounts:

1. Main Website anonymous visitor opens a published salon.
2. Book handoff opens `/app/customer/*` on the same apex origin.
3. Customer login persists while navigating Customer portal routes.
4. Owner login opens `/app/owner/*`; Customer/Growth Partner accounts are denied.
5. Growth Partner login opens `/app/partner/*`; other roles are denied.
6. Growth Partner receives a server-generated referral code.
7. Growth Partner submits a shop proposal; verify application/proposal rows.
8. Owner sees the proposal, publishes it, and attribution remains attached.
9. Customer sees the published salon through the shared catalog.
10. Complete a staging booking and verify the Owner booking, GP commission hold,
    and Customer booking status.
11. Confirm workers only control their own `/app/*/` path.
12. Confirm raw PWA URLs redirect to the apex canonical path.

Until this sequence passes against live Supabase and deployed origins, release
status remains **NO-GO** despite all repository-level checks passing.
